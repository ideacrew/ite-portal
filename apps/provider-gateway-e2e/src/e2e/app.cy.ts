/* eslint-disable @typescript-eslint/naming-convention */

describe('provider-gateway', () => {
  beforeEach(() => {});

  it('should ...', () => {
    // cy.visit('/provider-gateway');
    // cy.get('[data-cy="submit-new-bhsd"]').click();
    // cy.get('[data-cy="extract-date"]').should('exist');
  });
});
